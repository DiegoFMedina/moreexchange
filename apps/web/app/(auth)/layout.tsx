// PATH: apps/web/app/(auth)/layout.tsx
// DESC: Layout del grupo de rutas de autenticación — fondo oscuro, sin navbar

export default function AuthLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
